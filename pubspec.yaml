name: disaster_management_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.5.0   # or the latest version
  firebase_auth: ^5.3.0   # (optional, for login)
  cloud_firestore: ^5.4.0  # (optional, for database)
  firebase_messaging: ^15.0.0
  flutter_local_notifications: ^17.1.0
  charts_flutter: ^0.12.0
  fl_chart: ^1.1.1
  flutter_map: ^8.2.2
  latlong2: ^0.9.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
